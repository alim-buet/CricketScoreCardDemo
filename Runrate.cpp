#include <bits/stdc++.h>
using namespace std;
int main()
{
    cout << "Run: ";
    int run;
    cin >> run;
    cout << "Over: ";
    int over;
    cin >> over;
    cout << "Runrate: ";
    
    cout << (double)run / over << endl;
    
    
    

    return 0;
}